import { StyleSheet } from "react-native";

export const TabMenuStyle = StyleSheet.create({
  text: {
    fontSize: 15,
  },
  buttons: {
    flexDirection: "row",
    justifyContent: "space-evenly",
    alignItems: "center",
    flex: 1,
  },
});
