import { StyleSheet } from "react-native";

export const HeaderStyle = StyleSheet.create({
  image: {
    width: 170,
  },
  subText: {
    fontSize: 20,
    color: "#ABABAB",
    marginTop: -20,
  },
});
